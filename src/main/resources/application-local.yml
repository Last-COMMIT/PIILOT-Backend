spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/piilot
    username: ${LOCAL_DB_ID:lastcommit}
    password: ${LOCAL_DB_PW:secret}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true

  docker:
    compose:
      enabled: false

  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.lastcommit.piilot: debug
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace